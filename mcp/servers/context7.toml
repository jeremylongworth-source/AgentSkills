# Security note: this npx server can download and execute package code when the
# host starts it. Review and pin versions in controlled environments.
[mcp_servers.context7]
command = "npx.cmd"
args = ["-y", "@upstash/context7-mcp"]
