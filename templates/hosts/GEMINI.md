@AGENTS.md

## Gemini CLI

Use installed AgentSkills when a task matches a skill description.

Use `/memory show` to confirm loaded context files and `/skills reload` after
changing skill files during a session.
