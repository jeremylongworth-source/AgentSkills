# Scenario: Backend Auth Flow

Prompt:

> Plan the auth flow for API keys and service accounts. Include token lifecycle,
> permission checks, tenant isolation, audit events, error behavior, abuse
> cases, and tests for denied access.

Expected routing:

- auth-flow-design
- error-handling-contracts
- backend-test-plan
- skill-threat-model

Future bundle:

- backend-api
