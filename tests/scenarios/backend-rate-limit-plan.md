# Scenario: Backend Rate Limit Plan

Prompt:

> Design rate limits for public API endpoints by tenant and API key. Include
> quotas, burst behavior, headers, retry guidance, monitoring, support
> overrides, rollout, and tests.

Expected routing:

- rate-limit-design
- error-handling-contracts
- backend-test-plan
- skill-threat-model

Future bundle:

- backend-api
