# Scenario: DevOps CI Container

Prompt:

> Design a CI workflow and containerization plan for this backend service.
> Include quality gates, artifacts, cache, permissions, secrets, image build,
> runtime user, healthcheck, and validation steps.

Expected routing:

- ci-workflow-plan
- containerization-plan
- environment-config-review
- backend-test-plan

Future bundle:

- devops-cloud-release
