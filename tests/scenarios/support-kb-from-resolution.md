# Scenario: Support KB From Resolution

Prompt:

> This ticket was resolved after support found that reports fail to export when a workspace has archived fields. Draft a reusable KB article from the resolution.

Expected routing:

- kb-from-resolution
- concise-technical-writing

Future bundle:

- support-success
